<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tfxing.persondaily.dao.AnswerMapper">

    <select id="testForEach" resultType="com.tfxing.persondaily.entity.po.Answer">
        select * from answer
        <where>
            <foreach collection="list" item="item">

            </foreach>
        </where>
    </select>
</mapper>